settlement = {}

settlement.generate = function()
  local features = {11, 12, 117, 137, 138, 351}
  local feature = random(features)

  local data = {4,5,8,9}
  local width = 2
  local height = 2
  -- return {width, height, data}

  return feature
end